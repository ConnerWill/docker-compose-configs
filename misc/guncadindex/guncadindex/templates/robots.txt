{% load settings %}
#
# {% settings_value 'GUNCAD_SITE_NAME' %}
# {% settings_value 'GUNCAD_SITE_TAGLINE' %}
#
# robots.txt
#
# Contact information:
#   General:     {% settings_value 'GUNCAD_ADMIN_CONTACT' %}
#   Twitter:     {% settings_value 'GUNCAD_ADMIN_TWITTER' %}
#   Matrix:      {% settings_value 'GUNCAD_ADMIN_CHAT' %}
#
# Version:       {% settings_value 'GUNCAD_COMMIT_REF' %}
# Site URL:      {{ request.scheme }}://{{ request.get_host }}
# Source code:   {% settings_value 'GUNCAD_GIT_URL' %}
#

# For all user-agents, institute some level of rate limiting and bar off URLs
User-agent: *
Crawl-delay: 5
Allow: /
Disallow: /admin		# Admin UI is not Google's concern
Disallow: /api			# The traversable API is for robits, not people
Disallow: /metrics		# Robots don't need to see Prometheus metrics tyvm
Disallow: /out			# This is just used for external links, should not be indexed
Disallow: /search?		# Disallow search queries. Bots should traverse the sitemap for this info
Disallow: /vote			# Voting pages should be ignored to avoid accidental erroneous submissions

# Disallow traversal by the Common Crawl bot, omitting us from training data
# Note: this is NOT to be confused with GPTBot, which is for plugins and interactive use
User-agent: CCBot
Disallow: /

# Disallow traversal by GPTBot, omitting us from GPT training data
User-agent: GPTBot
Disallow: /

Sitemap: {{ request.scheme }}://{{ request.get_host }}{% url 'sitemap' %}
