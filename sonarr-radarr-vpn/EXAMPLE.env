# Identity
PUID=1000
PGID=1000
TZ=America/Chicago

# Paths
MEDIA_ROOT=/srv/media
DOWNLOADS_ROOT=/srv/downloads

# VPN core
VPN_SERVICE_PROVIDER=
VPN_TYPE=wireguard

# Allow your LAN to reach the UIs via gluetun published ports
FIREWALL_OUTBOUND_SUBNETS=192.168.1.0/24

# Optional
GLUETUN_LOG_LEVEL=info

# OpenVPN (if using openvpn)
OPENVPN_USER=
OPENVPN_PASSWORD=

# WireGuard (if using wireguard)
WIREGUARD_PRIVATE_KEY=
WIREGUARD_ADDRESSES=10.64.222.21/32

# Optional server selection (provider-dependent)
SERVER_COUNTRIES=
SERVER_REGIONS=
SERVER_CITIES=
